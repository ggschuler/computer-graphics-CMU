int alg(int vertXYZ) {
  xyz = vertXYZ - camera;
  u = vertX/z;
  v = vertY/z;
}

int drawLine() {
  //between (u1,v1) and (u2,v2)
}

int main() {

  int camera = (2,3,5);
  
  int vertA = (1,1,1);
  int vertB = (-1,1,1);
  int vertC = (1,-1,1);
  int vertD = (-1,-1,1);
  int vertE = (1,1,-1);
  int vertF = (-1,1,-1);
  int vertG = (1,-1,-1);
  int vertH = (-1,-1,-1);
}
